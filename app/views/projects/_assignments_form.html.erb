<%= form_for(@project) do |f| %>
<%= f.error_messages %>
  
  <a href="javascript:void(0)" id="edit-assignments">Add / Remove</a>

  <div id="token-holder">
  	<p>
    	<%= f.text_field :user_tokens, data: {load: @project.users} %>
    </p>
  </div>
  <%= f.submit %>
<% end %>
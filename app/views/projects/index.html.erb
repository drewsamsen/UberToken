<h2>Projects</h2>

<% content_for :sidebar do %>
	<ul>
		<% @projects.each do |project| %>
			<li><%= link_to project.name, project %></li>
		<% end %>
	</ul>

	<%= link_to "Create a workspace", new_project_path, :class => "create_new" %>
<% end %>
 | 
<%= link_to "New User", new_user_path %> |
<%= link_to "All Users", users_path %>